# D2.5 喂饭教程:跟着做就能跑通

> **配套**: 这是 D2.5 任务说明的 step-by-step 实施指南
> **目标读者**: 想先把工具跑通,再回头理解设计原理的学生
> **预计耗时**: 90 分钟(纯操作时间,不含理解代码)
> **风格**: 直接给最终代码,复制粘贴即可

> ⚠️ **本文档不解释"为什么"** — 那是任务说明 + D2.5 主文档(撞墙版)的事情。本文档只回答"怎么做"。建议**做完一遍跑通之后,回头翻一翻主文档,理解每一处设计为什么这么写**。

---

## 📌 跨平台命令说明

下文区分平台时用并列代码块,完全相同的命令(`git` / `python` / `pip` / `odp-init`)只写一次。

```
🐧 Linux / macOS / Git Bash:
    <bash 命令>

🪟 Windows PowerShell:
    <PowerShell 命令>
```

---

## Step 0:准备

### 0.1 确认环境

```bash
python --version       # 应当 ≥ 3.12
pip install -e ./apps/platform   # D2 应该已经装好
odp-init               # 跑一次,确保 D2 的 init_project 工作
```

### 0.2 推到远程(强烈推荐)

```bash
# 如果你还没建远程仓库,先去 GitHub/Gitee 建一个空 repo
git remote -v
# 如果空,加一个:
# git remote add origin <你的远程 URL>
git push -u origin main
```

> 💡 这一步不是必须,但建议做。出问题时可以 `git reset --hard origin/main` 回滚。

### 0.3 确认起点干净

```bash
git status
# 应当看到:nothing to commit, working tree clean
```

如果有未提交的改动,先处理掉(commit 或 stash)再开始。

---

## Step 1:改造 `paths.py`

打开 `apps/platform/src/odp_platform/common/paths.py`。

### 1.1 在文件末尾(或合适位置)追加 META_DIR 常量

```python
# ============================================================
# 工具元数据目录(audit trail / 工具自身日志)
# ============================================================
# 与业务日志 LOGGING_DIR 严格分开 —— 因为 reset 工具自己也会写日志,
# 如果日志写到 LOGGING_DIR(reset 要删的目录),工具会删自己的日志文件:
#   - Windows: WinError 32
#   - Linux:   静默丢日志(orphan inode)
# 同时审计追踪也要求"破坏性操作的记录不能被该操作销毁"。
META_DIR: Path = ROOT_DIR / ".odp-meta"
META_LOGGING_DIR: Path = META_DIR / "logs"
```

### 1.2 把 `META_LOGGING_DIR` 加进 `get_dirs_to_initialize()`

找到原本的 `get_dirs_to_initialize()`,在 return 列表末尾加一行:

```python
def get_dirs_to_initialize() -> list[Path]:
    return [
        # ... 原有目录保持不变 ...

        # 工具元数据(reset 等元工具的审计日志位置)
        META_LOGGING_DIR,
    ]
```

### 1.3 新增 `get_dirs_to_reset()` 函数

在 `get_dirs_to_initialize()` 下面加一个新函数:

```python
def get_dirs_to_reset() -> list[Path]:
    """返回 reset_project 可以安全清理的目录列表。

    安全原则:
        ✅ 只列【不进 git】的运行时产物目录
        ❌ 任何 git 追踪的目录(代码/文档/测试/配置)绝不列入
        ❌ RAW_DATA_DIR / PRETRAINED_MODELS_DIR 绝不列入
        ❌ META_LOGGING_DIR 绝不列入(审计追踪不能被审计对象自己销毁)
    """
    return [
        # 划分后的数据集(由数据转换脚本产生,不进 git)
        TRAIN_DIR,
        VAL_DIR,
        TEST_DIR,

        # 训练产物(不进 git)
        RUNS_DIR,
        CHECKPOINTS_DIR,

        # 端私有运行时日志(不进 git)
        LOGGING_DIR,
    ]
```

### 1.4 新增 `PROTECTED_DIRS` 常量

在 `get_dirs_to_reset()` 下面加:

```python
# ============================================================
# 绝对保护目录 —— reset 工具永远不能删除这些
# ============================================================
# 这是【最后一道安全网】—— 即使 get_dirs_to_reset() 列表写错了,
# reset 工具也会拒绝删除这些目录。
PROTECTED_DIRS: tuple[Path, ...] = (
    ROOT_DIR,                    # workspace 根
    ROOT_DIR / "apps",           # apps 容器
    ROOT_DIR / "packages",       # packages 容器
    APP_DIR,                     # platform 端根
    APP_DIR / "src",             # 源码目录
    SCRIPTS_DIR,                 # 进 git 的脚本
    DOCS_DIR,                    # 进 git 的文档
    UNIT_TEST_DIR,               # 进 git 的测试
    CONFIGS_DIR,                 # 进 git 的配置
    ROOT_DIR / ".git",           # git 元数据
    ROOT_DIR / ".odp-workspace", # marker file
    META_DIR,                    # 工具元数据根
    META_LOGGING_DIR,            # 工具自身日志
)
```

### 1.5 新增 `is_protected()` 函数

在 `PROTECTED_DIRS` 下面加:

```python
def is_protected(path: Path) -> bool:
    """检查路径是否在保护清单中。

    保护规则(任一满足都视为受保护):
        1. 路径本身在 PROTECTED_DIRS 中
        2. 路径是某个 PROTECTED_DIRS 成员的【祖先目录】
           (例如 ROOT_DIR 是 SCRIPTS_DIR 的祖先,
            如果允许删 ROOT_DIR,就连带删了 SCRIPTS_DIR)
    """
    path = path.resolve(strict=False)
    for protected in PROTECTED_DIRS:
        protected_resolved = protected.resolve(strict=False)
        if path == protected_resolved:
            return True
        if protected_resolved.is_relative_to(path):
            return True
    return False
```

> ⚠️ **注意**: `is_relative_to()` 是 Python 3.9+ 的方法。如果你的 Python 版本低于 3.9,得用 `try: protected_resolved.relative_to(path); return True; except ValueError: pass`。但任务前置要求是 3.12,所以应该没问题。

### 1.6 验证 `paths.py` 的改动

```bash
python -c "from odp_platform.common.paths import get_dirs_to_reset, PROTECTED_DIRS, is_protected, META_LOGGING_DIR; print('OK')"
```

应该输出 `OK`。如果报 `ImportError` / `NameError`,检查上面 5 段代码有没有完整粘贴、缩进对不对、有没有漏定义某个目录常量。

---

## Step 2:创建 `cli/reset_project.py`

新建文件 `apps/platform/src/odp_platform/cli/reset_project.py`,**完整内容如下**(直接复制覆盖):

```python
#!/usr/bin/env python
# -*- coding:utf-8 -*-
# @FileName  : reset_project.py
# @Author    : ODPlatform team
# @Project   : ODPlatform
# @Function  : 项目重置工具
"""ODPlatform 项目重置工具 —— 安全地撤销 init_project 创建的运行时产物。

设计要点:
    1. 默认 dry-run(显式 --yes 才执行)
    2. 必须打字 'RESET' 才删除(--force 跳过交互)
    3. 双重防护:get_dirs_to_reset 白名单 + is_protected 黑名单
    4. 进度反馈 + 大目录预警
    5. Windows 只读文件兼容(_on_rm_error)
    6. 日志写在 META_LOGGING_DIR,不在自己要删的目录
    7. 审计上下文(user / git_rev / argv / cwd)随每次运行记录
"""

from __future__ import annotations

import argparse
import getpass
import os
import shutil
import stat
import subprocess
import sys
from pathlib import Path

from odp_platform.common.paths import (
    ROOT_DIR,
    META_LOGGING_DIR,
    RAW_DATA_DIR,
    PRETRAINED_MODELS_DIR,
    get_dirs_to_reset,
    is_protected,
)
from odp_platform.common.logging_utils import get_logger
from odp_platform.common.string_utils import format_table_row, format_table_separator


logger = get_logger(
    base_path=META_LOGGING_DIR,
    log_type="reset_project",
    temp_log=False,
)


CONFIRM_KEYWORD = "RESET"
LINE_WIDTH = 70


# ─────────────────────────────────────────────────────────
# 工具函数
# ─────────────────────────────────────────────────────────

def _format_size(bytes_size: int) -> str:
    """二进制单位(GiB/MiB/KiB)—— 统一用 1024 进制以避免单位混淆"""
    if bytes_size >= 1024 ** 3:
        return f"{bytes_size / (1024 ** 3):.2f} GiB"
    if bytes_size >= 1024 ** 2:
        return f"{bytes_size / (1024 ** 2):.2f} MiB"
    if bytes_size >= 1024:
        return f"{bytes_size / 1024:.2f} KiB"
    return f"{bytes_size} B"


def _on_rm_error(func, path, exc_info):
    """rmtree 错误回调 —— 处理 Windows 只读文件场景。

    Windows 上某些被 git checkout 出来的文件会被标记只读,
    shutil.rmtree 直接 PermissionError。改成可写后重试。
    Linux 上 chmod + retry 也是无害的,所以可以跨平台用。
    """
    try:
        os.chmod(path, stat.S_IWRITE)
        func(path)
    except OSError:
        raise


def _audit_context() -> dict:
    """收集审计上下文 —— user / git_rev / argv / cwd / pid。"""
    try:
        git_rev = subprocess.check_output(
            ["git", "rev-parse", "HEAD"], cwd=ROOT_DIR, text=True,
            stderr=subprocess.DEVNULL,
        ).strip()
    except (subprocess.CalledProcessError, FileNotFoundError):
        git_rev = "(not a git repo)"
    return {
        "user": getpass.getuser(),
        "pid": os.getpid(),
        "git_rev": git_rev,
        "argv": sys.argv,
        "cwd": os.getcwd(),
    }


# ─────────────────────────────────────────────────────────
# 扫描 / 计划展示 / 确认 / 删除
# ─────────────────────────────────────────────────────────

def _scan_targets() -> tuple[list[tuple[Path, int, int]], list[Path]]:
    """扫描所有目标,返回 (可删除的, 跳过的)。

    可删除项是 (path, file_count, total_size_bytes) 三元组。
    """
    deletable: list[tuple[Path, int, int]] = []
    skipped: list[Path] = []

    for d in get_dirs_to_reset():
        if is_protected(d):
            logger.warning(f"⛔ 拒绝处理受保护目录(配置可能有误): {d}")
            skipped.append(d)
            continue

        if not d.exists():
            skipped.append(d)
            continue

        file_count = 0
        total_size = 0
        try:
            for f in d.rglob("*"):
                if f.is_file():
                    file_count += 1
                    try:
                        total_size += f.stat().st_size
                    except OSError:
                        pass
        except OSError as e:
            logger.warning(f"扫描 {d} 时出错: {e}")

        deletable.append((d, file_count, total_size))

    return deletable, skipped


def _print_plan(
    deletable: list[tuple[Path, int, int]],
    skipped: list[Path],
    will_actually_delete: bool,
) -> None:
    if will_actually_delete:
        logger.warning("⚠️  即将删除以下目录".center(LINE_WIDTH, '='))
    else:
        logger.info("📋 [DRY-RUN] 计划如下(未实际删除)".center(LINE_WIDTH, '='))

    if not deletable:
        logger.info("(没有可删除的目录 —— 项目已经是干净状态)")
        return

    widths = [40, 12, 14]
    aligns = ['left', 'right', 'right']
    logger.info(format_table_row(['目录', '文件数', '大小'], widths, aligns))
    logger.info(format_table_separator(widths))

    total_files = 0
    total_bytes = 0
    for path, count, size in deletable:
        rel = path.relative_to(ROOT_DIR)
        logger.info(format_table_row(
            [str(rel), str(count), _format_size(size)],
            widths, aligns,
        ))
        total_files += count
        total_bytes += size

    logger.info(format_table_separator(widths))
    logger.info(format_table_row(
        ['【合计】', str(total_files), _format_size(total_bytes)],
        widths, aligns,
    ))

    logger.info("")
    logger.info("✅ 以下重要目录【不会】被动:")
    logger.info(f"  - 原始数据: {RAW_DATA_DIR.relative_to(ROOT_DIR)}/")
    logger.info(f"  - 预训练权重: {PRETRAINED_MODELS_DIR.relative_to(ROOT_DIR)}/")
    logger.info(f"  - 所有代码、文档、配置(进 git 的)")


def _confirm(deletable_count: int) -> bool:
    """交互式确认。

    用 print 而非 logger,这是【刻意的视觉打断】——
    让用户的眼睛从"扫日志"模式切换到"主动决策"模式。
    """
    print()
    print("=" * LINE_WIDTH)
    print(f"⚠️  你正要删除 {deletable_count} 个目录的内容。这个操作不可撤销。")
    print(f"⚠️  如果确认,请精确输入大写的 '{CONFIRM_KEYWORD}'(其他任何输入都会取消):")
    print("=" * LINE_WIDTH)
    try:
        user_input = input("> ").strip()
    except (KeyboardInterrupt, EOFError):
        print()
        return False
    return user_input == CONFIRM_KEYWORD


def _delete_one(
    path: Path,
    idx: int,
    total: int,
    file_count: int,
    size: int,
) -> str | None:
    """删除单个目录,带进度提示。返回 None=成功,字符串=失败原因"""
    if is_protected(path):
        logger.error(f"[{idx}/{total}] ⛔ 删除前检查失败,跳过: {path}")
        return "受保护目录"

    rel = path.relative_to(ROOT_DIR)
    size_str = _format_size(size)

    if size > 1024 ** 3:  # > 1 GiB
        logger.warning(
            f"[{idx}/{total}] 正在删除 {rel} ({size_str}, {file_count} 个文件)"
            f"—— 这可能需要一会..."
        )
    else:
        logger.info(f"[{idx}/{total}] 删除 {rel} ({size_str}, {file_count} 个文件)")

    try:
        shutil.rmtree(path, onerror=_on_rm_error)
        logger.info(f"[{idx}/{total}] ✅ 已删除: {rel}")
        return None
    except OSError as e:
        logger.error(f"[{idx}/{total}] ❌ 删除失败 {rel}: {e}")
        return str(e)


def _execute_delete(deletable: list[tuple[Path, int, int]]) -> None:
    total = len(deletable)
    success: list[Path] = []
    failed: list[tuple[Path, str]] = []

    for idx, (path, file_count, size) in enumerate(deletable, 1):
        reason = _delete_one(path, idx, total, file_count, size)
        if reason is None:
            success.append(path)
        else:
            failed.append((path, reason))

    logger.info("=" * LINE_WIDTH)
    if failed:
        logger.warning(f"完成: 成功 {len(success)} 个,失败 {len(failed)} 个")
        for p, reason in failed:
            logger.warning(f"  - {p.relative_to(ROOT_DIR)}: {reason}")
    else:
        logger.info(f"完成: 成功 {len(success)} 个,失败 0 个")


# ─────────────────────────────────────────────────────────
# 主流程
# ─────────────────────────────────────────────────────────

def reset_project(yes: bool = False, force: bool = False, dry_run: bool = False) -> int:
    """重置项目入口。

    Args:
        yes:     --yes,显式同意删除
        force:   --force,跳过交互式确认
        dry_run: --dry-run,显式声明只打印不删除

    --dry-run 与 --yes 互斥;同时给以 --dry-run 优先并警告。

    Returns:
        退出码(0=成功 / dry-run,1=取消或失败)
    """
    logger.info("项目重置工具".center(LINE_WIDTH, '='))
    logger.info(f"项目根目录: {ROOT_DIR}")

    ctx = _audit_context()
    logger.info(
        f"审计上下文: user={ctx['user']} pid={ctx['pid']} "
        f"git={ctx['git_rev'][:8]} argv={ctx['argv']}"
    )

    if dry_run and yes:
        logger.warning("⚠️  同时给了 --dry-run 和 --yes,以 --dry-run 为准(只打印不删除)")
        yes = False

    deletable, skipped = _scan_targets()
    _print_plan(deletable, skipped, will_actually_delete=yes)

    if not deletable:
        return 0

    if not yes:
        logger.info("")
        if dry_run:
            logger.info("💡 这是显式的 --dry-run。要真正执行删除,请加 --yes:")
        else:
            logger.info("💡 这是 dry-run(默认行为)。要真正执行删除,请加 --yes:")
        logger.info("   python scripts/reset_project.py --yes")
        return 0

    if not force:
        if not _confirm(len(deletable)):
            logger.warning("❌ 用户取消,未执行删除")
            return 1

    logger.info("")
    logger.info("开始删除...".center(LINE_WIDTH, '='))
    _execute_delete(deletable)
    return 0


def main() -> int:
    parser = argparse.ArgumentParser(
        description="重置 ODPlatform 项目 —— 撤销 init_project 创建的运行时产物。",
        formatter_class=argparse.RawDescriptionHelpFormatter,
        epilog="""
示例:
  odp-reset                      # 默认 dry-run(推荐先跑这个)
  odp-reset --yes                # 真删除,带交互式确认
  odp-reset --yes --force        # CI / 脚本调用,跳过交互
  odp-reset --dry-run            # 显式 dry-run

注意:
  - 永远不删除 data/raw/(原始数据)
  - 永远不删除代码、文档、配置(任何进 git 的目录)
  - 审计日志写在 .odp-meta/logs/reset_project/,不会被自身清理
""",
    )
    parser.add_argument("--yes", action="store_true",
                        help="真正执行删除(默认是 dry-run)")
    parser.add_argument("--force", action="store_true",
                        help="跳过交互式确认(仅当 --yes 时有效)")
    parser.add_argument("--dry-run", action="store_true",
                        help="显式声明 dry-run(默认行为也是 dry-run,但显式更清楚)")
    args = parser.parse_args()
    return reset_project(yes=args.yes, force=args.force, dry_run=args.dry_run)


if __name__ == "__main__":
    sys.exit(main())
```

---

## Step 3:创建脚本入口

新建文件 `scripts/reset_project.py`,内容如下:

```python
#!/usr/bin/env python
# -*- coding:utf-8 -*-
"""ODPlatform 项目重置工具 —— 命令行入口(薄壳)。

实际逻辑在 odp_platform.cli.reset_project.main 里。
"""

import sys
from pathlib import Path

REPO_ROOT = Path(__file__).resolve().parent.parent
sys.path.insert(0, str(REPO_ROOT / "apps" / "platform" / "src"))

from odp_platform.cli.reset_project import main

if __name__ == "__main__":
    sys.exit(main())
```

> ⚠️ **入口必须调 `main()`,不能调 `reset_project()`**。前者走 argparse 解析命令行参数,后者会绕过 argparse 拿到全部默认值(所有 flag 都失效)。

---

## Step 4:更新 `.gitignore`

在 `.gitignore` **末尾**追加:

```gitignore

# 工具元数据(reset/doctor 等元工具的审计日志,本地运行历史)
.odp-meta/
```

---

## Step 5:注册 `odp-reset` 命令

打开 `apps/platform/pyproject.toml`,找到 `[project.scripts]` 段,加一行:

```toml
[project.scripts]
odp-init  = "odp_platform.cli.init_project:initialize_project"
odp-reset = "odp_platform.cli.reset_project:main"
```

注意 `odp-reset` 指向的是 **`main`**(它做 argparse 解析),不是 `reset_project`。

---

## Step 6:重新装包 + 验证

### 6.1 重装

```bash
pip install -e ./apps/platform --force-reinstall --no-deps
```

加 `--force-reinstall --no-deps` 是因为 entry-point 改了,要让 pip 重新生成命令脚本。

### 6.2 准备一份"运行时产物"用于测试

```bash
odp-init     # 让 META_LOGGING_DIR (.odp-meta/logs/) 也被建出来
```

🐧 Linux/macOS 造一些假数据:
```bash
mkdir -p data/train data/val data/test runs/exp_0 models/checkpoints apps/platform/logging
for d in data/train data/val data/test runs/exp_0 models/checkpoints apps/platform/logging; do
    echo "test" > "$d/dummy.txt"
done
```

🪟 Windows PowerShell 造一些假数据:
```powershell
New-Item -ItemType Directory -Force data/train, data/val, data/test, runs/exp_0, models/checkpoints, apps/platform/logging | Out-Null
foreach ($d in "data/train","data/val","data/test","runs/exp_0","models/checkpoints","apps/platform/logging") {
    "test" | Set-Content "$d/dummy.txt"
}
```

### 6.3 验证 1:默认 dry-run

```bash
odp-reset
```

📺 你应该看到类似:

```
================== 项目重置工具 ==================
项目根目录: /.../ODPlatform
审计上下文: user=... pid=... git=abcd1234 argv=['/...bin/odp-reset']
======= 📋 [DRY-RUN] 计划如下(未实际删除) =======
目录                         文件数            大小
─────────────────────────────────────────────────
data/train                       1            5 B
data/val                         1            5 B
data/test                        1            5 B
runs                             1            5 B
models/checkpoints               1            5 B
apps/platform/logging            1            5 B
─────────────────────────────────────────────────
【合计】                          6           30 B

✅ 以下重要目录【不会】被动:
  - 原始数据: data/raw/
  - 预训练权重: models/pretrained/
  - 所有代码、文档、配置(进 git 的)

💡 这是 dry-run(默认行为)。要真正执行删除,请加 --yes:
   python scripts/reset_project.py --yes
```

**没有任何东西被删**。如果 `data/train` 之类的目录消失了,说明 `--yes` 的默认值有问题,回去检查 Step 2 代码。

### 6.4 验证 2:--yes 但取消

```bash
odp-reset --yes
# 看到二次确认提示,输入 'y' 回车 (注意不是 RESET)
```

📺 你应该看到:

```
============ ⚠️  即将删除以下目录 ============
...(表格)...

======================================================================
⚠️  你正要删除 6 个目录的内容。这个操作不可撤销。
⚠️  如果确认,请精确输入大写的 'RESET'(其他任何输入都会取消):
======================================================================
> y
❌ 用户取消,未执行删除
```

确认 `data/train` 等目录**还在**。

### 6.5 验证 3:--yes 并确认

```bash
odp-reset --yes
# 这次输入 RESET 回车
```

📺 你应该看到逐目录的 `[1/6] 删除 ...` `[1/6] ✅ 已删除` 这样的进度,最后 `完成: 成功 6 个,失败 0 个`。

确认 `data/train` 等目录**已被删除**,但 `data/raw/`(如果存在)**还在**。

### 6.6 验证 4:--yes --force(免交互)

先重新造数据(重复 6.2),然后:

```bash
odp-reset --yes --force
```

应该不需要任何交互,直接完成删除。

### 6.7 验证 5:审计日志

```bash
ls .odp-meta/logs/reset_project/
```

应该看到至少一个 `.log` 文件。打开看看:

🐧 `cat .odp-meta/logs/reset_project/*.log | head -20`
🪟 `Get-Content .odp-meta/logs/reset_project/*.log -Head 20`

应该能看到 `审计上下文: user=... pid=... git=... argv=[...]` 这一行。

### 6.8 验证 6:`git status` 干净

```bash
git status
```

应该**没有任何已追踪文件被改动**。如果有 modified 的 git-tracked 文件,说明你的 `get_dirs_to_reset()` 或 `is_protected()` 有问题——立即停下来检查 Step 1。

`.odp-meta/` **不应该出现**在 untracked 列表里(被 `.gitignore` 排除)。

---

## Step 7:Git 提交

按 5 次 commit 的节奏分批提交。**先把你刚才造的测试数据清掉**:

```bash
# 重新造数据测试时留下的 dummy 目录,如果还在就清掉
rm -rf data/train data/val data/test runs models/checkpoints apps/platform/logging
# 或者直接再跑一遍 odp-reset --yes --force
```

### Commit 1:paths.py 加 reset 目标清单 + 保护机制

```bash
git add apps/platform/src/odp_platform/common/paths.py
```

但等一下——这次提交只想包含 `get_dirs_to_reset` / `PROTECTED_DIRS` / `is_protected` 这一组改动,**不包含 META_DIR**(那个留给 Commit 2)。

如果你想严格做到这点,可以用 `git add -p` 交互式分块挑选;或者更简单的做法是 **Commit 1 和 Commit 2 合并成一次**,提交所有 paths.py 改动:

```bash
git add apps/platform/src/odp_platform/common/paths.py
git commit -m "feat(common): 在 paths.py 加入 reset 目标清单 + 保护机制 + META_DIR

新增三组定义,共同支撑安全的 reset_project 工具:

1. get_dirs_to_reset(): 显式白名单,列出可被清理的运行时产物目录。
   - 与 get_dirs_to_initialize 不同,绝不含 git 追踪的目录。

2. PROTECTED_DIRS + is_protected(): 兜底黑名单,阻止任何对
   workspace 根、代码、文档、.git 等关键目录的删除——即使白名单
   被错误配置也拦得住。is_protected 做双向检查(自身 + 祖先关系)。

3. META_DIR / META_LOGGING_DIR: 工具自身的元数据目录,与业务日志
   严格分离。reset 工具的审计日志写在这里,确保'破坏性操作的记录
   不能被该操作销毁'(对应 SOX/HIPAA/ISO 27001 的审计追踪要求)。
   - 加入 get_dirs_to_initialize (启动时建出来)
   - 加入 PROTECTED_DIRS (任何 reset 都不能动)
   - 明确不加入 get_dirs_to_reset
"
```

### Commit 2:.gitignore 加 `.odp-meta/`

```bash
git add .gitignore
git commit -m "chore: 把 .odp-meta/ 加入 gitignore(本地元工具历史)"
```

### Commit 3:新建 reset_project.py + scripts 入口

```bash
git add apps/platform/src/odp_platform/cli/reset_project.py
git add scripts/reset_project.py
git commit -m "feat(cli): 新建 reset_project 项目重置工具

提供 init_project 运行时产物的安全撤销操作。

安全设计(对齐业界标准 git clean -n / terraform plan /
kubectl delete --dry-run):

1. 默认 dry-run(不删,只打印计划)
2. 真删必须显式加 --yes
3. 交互式二次确认:必须打字 'RESET'(区分大小写)
4. --force 跳过交互(用于 CI / 自动化)
5. --dry-run 显式 flag,与 --yes 冲突时优先且警告
6. 双重保护:白名单 + 黑名单(见 paths.py)
7. 逐目录进度反馈 + >1GiB 目标的预警
8. Windows 只读文件兼容(shutil.rmtree onerror 回调)
9. 审计上下文(user/git_rev/argv/cwd/pid)每次运行随日志记录
10. 日志写在 META_LOGGING_DIR,不在自己要删的目录

绝不会触碰:
- data/raw/(用户标注数据)
- models/pretrained/(可下载但代价高)
- 任何 git 追踪过的目录(代码、文档、配置)

scripts/reset_project.py 是薄壳入口,委托给 cli.reset_project.main
确保 argparse 正确解析命令行参数。
"
```

### Commit 4:注册 odp-reset 命令

```bash
git add apps/platform/pyproject.toml
git commit -m "feat(platform): 注册 odp-reset 控制台命令

将 odp-reset 加为 odp-init 的姊妹命令,凑齐
项目生命周期管理所需的 init/reset 对称命令对。
"
```

### 推到远程

```bash
git push origin main
```

---

## Step 8:最终确认

```bash
git log --oneline | head -6
```

应该看到 4 次新 commit:

```
xxxxxxx (HEAD -> main, origin/main) feat(platform): 注册 odp-reset 控制台命令
xxxxxxx feat(cli): 新建 reset_project 项目重置工具
xxxxxxx chore: 把 .odp-meta/ 加入 gitignore(本地元工具历史)
xxxxxxx feat(common): 在 paths.py 加入 reset 目标清单 + 保护机制 + META_DIR
... (D2 留下的 commit)
```

最后一次完整冒烟测试:

```bash
odp-init                      # 重建运行时目录
odp-reset                     # dry-run 看计划
odp-reset --yes --force       # 真删
ls .odp-meta/logs/reset_project/   # 确认审计日志存在
git status                    # 应该是 clean
```

🎉 **完成**!

---

## 常见问题

### Q1: `odp-reset: command not found`

**A**: 没装包或者装包失败。重跑:

```bash
pip install -e ./apps/platform --force-reinstall --no-deps
```

### Q2: 跑 `odp-reset --yes` 但表现像 dry-run

**A**: 极大概率是 `scripts/reset_project.py` 调成 `reset_project()` 而不是 `main()` 了。打开看一眼,必须是:

```python
from odp_platform.cli.reset_project import main
if __name__ == "__main__":
    sys.exit(main())
```

如果不是 `main`,改过来。

### Q3: `ImportError: cannot import name 'META_LOGGING_DIR' from 'odp_platform.common.paths'`

**A**: Step 1.1 的 META_DIR 常量没加上,或者打错了名字。回去检查。

### Q4: Windows 上跑完 reset 报 `WinError 32`

**A**: 你的 `cli/reset_project.py` 里 `get_logger` 的 `base_path` 没改成 `META_LOGGING_DIR`,还是 `LOGGING_DIR`。这意味着 reset 在试图删自己的日志文件——回 Step 2,找到这两行:

```python
from odp_platform.common.paths import (
    ...
    META_LOGGING_DIR,           # ← 这里必须 import 这个
    ...
)

logger = get_logger(
    base_path=META_LOGGING_DIR, # ← 这里必须用 META_LOGGING_DIR
    ...
)
```

### Q5: `git status` 显示 `data/raw/` 里的东西被删了

**A**: 严重错误。立刻停下来检查 `get_dirs_to_reset()`,**绝对不能**包含 `RAW_DATA_DIR`。然后从远程恢复 `data/raw/`:

```bash
git checkout -- data/raw/   # 如果 raw 在 git 里
# 或者从你 0.2 步推的远程仓库 / 备份恢复
```

### Q6: 第一次跑 `odp-reset` 报 `FileNotFoundError: .odp-meta/logs/...`

**A**: `META_LOGGING_DIR` 没加进 `get_dirs_to_initialize()`,所以 `odp-init` 没建出来。回去 Step 1.2 检查;补好之后再跑一次 `odp-init`。

### Q7: 我想理解为什么这么设计

**A**: 翻 D2.5 主文档(撞墙版)。每一处设计都对应一次"如果不这么做会发生什么"的真实演练。建议跑通本喂饭教程**之后**再去看那份,带着实操经验看会非常有共鸣。

---

**搞定!如果上面所有步骤跑通,任务说明的"验收标准(DoD)"应该自动满足大半。剩下的"故意把 ROOT_DIR 加进白名单测保护机制"等加分项,值得花 10 分钟做一下——能让你对 `is_protected()` 的双向检查彻底建立信心。**
