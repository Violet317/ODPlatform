# ODPlatform D2(喂饭版):一步步搭出企业级项目初始化系统

> **本文用法**:从空文件夹开始,**照着每一步敲下去**,最终得到一个企业级、可安装、可移植的项目初始化系统(`odp-init` 命令)。
>
> 本文**只写正确操作**——不绕弯路、不做反面演示。每一步都说明**怎么做**和**为什么这么做**。照做即可,出现的结果应与文中一致。
>
> **建议时长**:4-6 课时。

---

## 跨平台命令约定(必读一次,后面不再重复)

本文命令分两种系统给出:

```
🐧 Linux / macOS / Git Bash:  <bash 命令>
🪟 Windows PowerShell:         <PowerShell 命令>
```

`git`、`python`、`pip` 这类命令跨平台一致,只写一次。

**Windows 用户最省心的方案**:安装 [Git for Windows](https://git-scm.com/download/win),用它自带的 **Git Bash**。这样本文所有 🐧 命令都能直接跑,**完全不用看 PowerShell 版本**。VSCode 里可按 `Ctrl+Shift+P` → `Terminal: Select Default Profile` → 选 `Git Bash`。

**关于创建文件**:本文给出命令行写法是为了"全程可复现",但**更推荐直接用编辑器(VSCode / PyCharm)新建文件、粘贴内容、保存**——尤其是几十上百行的代码文件。编辑器保存时**会自动创建沿途所有目录**,比命令行还快。保存编码统一选 **UTF-8(无 BOM)**。

---

## 准备工作:确认环境

确认 Python ≥ 3.10:

```
python --version
```

推荐用 conda 建一个干净环境(后续所有 `pip install` 都装进这个环境):

```
conda create -n odplat python=3.12
conda activate odplat
```

确认 git 已安装:

```
git --version
```

---

# 阶段 0:建立项目基线

**为什么先做这步**:在写任何业务代码之前,先把仓库的"规矩"立好——一个标记文件(让代码能定位项目根)和一份 `.gitignore`(防止垃圾文件被误提交)。一旦大文件被 commit 进 git,清理成本极高,所以**先立规矩,再写代码**。

**为什么不一次性 `mkdir` 19 个目录**:项目目录其实分三类,各有各的"出生时机",不需要预先全建出来:

| 类别 | 例子 | 出生时机 | 进 git 吗 |
|---|---|---|---|
| A. 代码/文档目录 | `.../common/` | 写第一个文件时,编辑器自动建出 | ✅ 随代码 |
| B. 占位目录(为未来端预留) | `apps/web-backend/` | 写它的 README 时顺带建 | ✅ 随 README |
| C. 运行时目录 | `data/raw/`、`runs/` | 由 `init_project` 在每个新环境创建 | ❌ 内容被 gitignore |

阶段 0 只处理"基线文件"。

## Step 0.1:创建项目目录 + git init

🐧 **Linux / macOS / Git Bash**:
```bash
mkdir ODPlatform
cd ODPlatform
git init
git config user.name "你的名字"
git config user.email "你的邮箱"
```

🪟 **Windows PowerShell**:
```powershell
mkdir ODPlatform
cd ODPlatform
git init
git config user.name "你的名字"
git config user.email "你的邮箱"
```

## Step 0.2:创建 marker file(`.odp-workspace`)

**为什么需要它**:后面 `paths.py` 要定位"项目根目录"。Monorepo 目录很深,如果用 `parent.parent.parent...` 数层级,一旦目录结构调整就全乱。正确做法是放一个**标记文件**在仓库根,代码从任意位置**向上查找这个文件**来定位根目录——这个文件就是 `.odp-workspace`。

**为什么叫这个名**:`.` 开头表示它是配置/标记文件(类似 `.git`);`workspace` 强调这是整个 Monorepo 的根。这种"放标记文件向上找"是工业标准模式(`pytest`、`poetry`、`git`、`npm`、`cargo` 都这么做)。

**用编辑器**在仓库根创建 `.odp-workspace`,内容:

```
# ODPlatform 工作区标记文件
#
# 此空文件用于标记工作区根目录。
# paths.py 会从模块位置向上查找此文件，
# 使路径解析不依赖目录结构变化。
#
# 行业标准模式（pytest、poetry、git、npm、cargo 等均采用）。
#
# 请勿删除此文件。
```

> 💡 命令行写法(可选)。🐧 用 `cat > .odp-workspace << 'EOF' ... EOF`;🪟 PowerShell 用 `@'...'@ | Set-Content -Path .odp-workspace -Encoding UTF8`。Windows 10 自带的 PowerShell 5.1 的 `-Encoding UTF8` 会带 BOM,**建议直接用编辑器创建并存为 UTF-8 无 BOM**,最省事。

## Step 0.3:创建 `.gitignore`

**用编辑器**在仓库根创建 `.gitignore`,内容:

```gitignore
# ============================================================
# Python
# ============================================================
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
develop-eggs/
dist/
*.egg-info/
.eggs/

# ============================================================
# 虚拟环境
# ============================================================
venv/
env/
.venv/
ENV/

# ============================================================
# IDE / 编辑器
# ============================================================
.idea/
.vscode/
*.swp
*~

# ============================================================
# 操作系统
# ============================================================
.DS_Store
Thumbs.db

# ============================================================
# 测试 / 覆盖率
# ============================================================
.pytest_cache/
.coverage
.coverage.*
htmlcov/
.mypy_cache/
.ruff_cache/

# ============================================================
# ODPlatform 专属(Monorepo 路径)
# ============================================================

# 数据集不进 git(走外部存储或 Git LFS)
data/raw/
data/train/
data/val/
data/test/
!data/README.md
!data/.gitkeep

# 模型权重不进 git
models/pretrained/*.pt
models/checkpoints/*.pt
models/checkpoints/*/
!models/pretrained/README.md
!models/checkpoints/README.md

# 训练运行结果(每次自动产生新目录)
runs/*
!runs/README.md

# 端私有日志(运行时产生,各机器不同)
apps/platform/logging/
*.log

# 临时文件
tmp/
temp/

# ============================================================
# 大文件 / 二进制
# ============================================================
*.pkl
*.h5
*.npz
```

## Step 0.4:第一次 commit

```
git status
git add .gitignore .odp-workspace
git commit -m "chore: initial commit with .gitignore + .odp-workspace marker"
```

> commit message 写中文也可以:`git commit -m "chore: 初始提交，添加 .gitignore 和 .odp-workspace 标记文件"`

✅ **检查点**:`git log --oneline` 看到 **1 次** commit。仓库现在只有 2 个文件,没有任何空目录——这是有意为之。

---

# 阶段 1:`paths.py`(集中管理所有路径)

**为什么需要这个模块**:项目里到处要用路径(`data/raw`、`models/checkpoints`……)。如果每个脚本各自硬编码字符串,改一处要改 N 处,而且相对路径会随"当前工作目录"漂移。正确做法是:**所有路径常量集中在一个模块里定义一次**,别处只 import。

`paths.py` 解决两个核心问题:

1. **项目根怎么定位** → marker file 模式(从本文件位置向上找 `.odp-workspace`),不数层级,**重构目录也不会坏**。
2. **哪些资产共享、哪些端私有** → 引入 `APP_DIR` 概念。`data/`、`models/`、`runs/` 是所有端共享的(放在 `ROOT_DIR` 下);`logging/`、`configs/`、`tests/` 是某个端私有的(放在 `APP_DIR` 下)。**目录放在哪一层 = 声明它被谁共享**。

另一个关键设计:**`paths.py` 自己不做任何 I/O**(不创建目录),只定义常量。它只回答"要哪些目录",由 `init_project` 负责"创建"。

## Step 1.1:创建 `paths.py`

**用编辑器**创建 `apps/platform/src/odp_platform/common/paths.py`(编辑器会自动建出沿途目录):

```python
#!/usr/bin/env python
# -*- coding:utf-8 -*-
# @FileName  : paths.py
# @Project   : ODPlatform
# @Function  : 集中定义项目路径常量
#              - 使用 marker file 模式定位 ROOT_DIR(workspace 根)
#              - 引入 APP_DIR 概念,把【共享资产】和【端私有资产】分开

from pathlib import Path
from typing import List, Tuple

# ============================================================
# Workspace 根目录定位 (marker file 模式)
# ============================================================
WORKSPACE_MARKER: str = ".odp-workspace"


def _find_workspace_root(
    start: Path,
    markers: Tuple[str, ...] = (WORKSPACE_MARKER,),
) -> Path:
    """
    从 start 开始沿父目录向上查找,返回包含任一 marker 文件的目录。

    Args:
        start: 起始路径(通常是 Path(__file__))
        markers: 一组 marker 文件名,任一存在即视为找到

    Returns:
        Path: workspace 根目录

    Raises:
        FileNotFoundError: 一直爬到文件系统根仍没找到
    """
    current = start.resolve()
    if current.is_file():
        current = current.parent

    for parent in [current, *current.parents]:
        for marker in markers:
            if (parent / marker).exists():
                return parent

    raise FileNotFoundError(
        f"找不到 workspace marker 文件 ({markers})。"
        f"请确认仓库根存在 {WORKSPACE_MARKER} 文件。"
    )


# 计算 ROOT_DIR(模块加载时执行一次)
ROOT_DIR: Path = _find_workspace_root(Path(__file__))


# ============================================================
# 端根目录 APP_DIR (platform 这一个端的根)
# ============================================================
APP_DIR: Path = ROOT_DIR / "apps" / "platform"


# ============================================================
# 【共享资产】(在 ROOT_DIR 下,所有端可访问)
# ============================================================
DATA_DIR: Path = ROOT_DIR / "data"
MODELS_DIR: Path = ROOT_DIR / "models"
RUNS_DIR: Path = ROOT_DIR / "runs"

# 模型子目录
PRETRAINED_MODELS_DIR: Path = MODELS_DIR / "pretrained"
CHECKPOINTS_DIR: Path = MODELS_DIR / "checkpoints"

# 数据集子目录
RAW_DATA_DIR: Path = DATA_DIR / "raw"

TRAIN_DIR: Path = DATA_DIR / "train"
VAL_DIR: Path = DATA_DIR / "val"
TEST_DIR: Path = DATA_DIR / "test"

TRAIN_IMAGES_DIR: Path = TRAIN_DIR / "images"
TRAIN_LABELS_DIR: Path = TRAIN_DIR / "labels"
VAL_IMAGES_DIR: Path = VAL_DIR / "images"
VAL_LABELS_DIR: Path = VAL_DIR / "labels"
TEST_IMAGES_DIR: Path = TEST_DIR / "images"
TEST_LABELS_DIR: Path = TEST_DIR / "labels"


# ============================================================
# 【端私有资产】(在 APP_DIR 下,只属于 platform 这个端)
# ============================================================
CONFIGS_DIR: Path = APP_DIR / "configs"
LOGGING_DIR: Path = APP_DIR / "logging"
UNIT_TEST_DIR: Path = APP_DIR / "tests"


# ============================================================
# 【顶层文档目录】(共享给所有人)
# ============================================================
DOCS_DIR: Path = ROOT_DIR / "docs"


# ============================================================
# 【工程基础设施目录】(共享)
# ============================================================
SCRIPTS_DIR: Path = ROOT_DIR / "scripts"


# ============================================================
# 对外暴露的"要初始化的目录列表"
# ============================================================
def get_dirs_to_initialize() -> List[Path]:
    """
    返回项目启动时需要确保存在的所有目录列表。

    这是 init_project.py 的【唯一数据源】——
    paths.py 决定要哪些目录,init_project 只负责创建。

    Returns:
        所有需要初始化的目录路径列表
    """
    return [
        # 共享资产
        DATA_DIR,
        RUNS_DIR,
        MODELS_DIR,
        PRETRAINED_MODELS_DIR,
        CHECKPOINTS_DIR,
        RAW_DATA_DIR,
        TRAIN_IMAGES_DIR,
        TRAIN_LABELS_DIR,
        VAL_IMAGES_DIR,
        VAL_LABELS_DIR,
        TEST_IMAGES_DIR,
        TEST_LABELS_DIR,
        # 端私有资产
        CONFIGS_DIR,
        LOGGING_DIR,
        UNIT_TEST_DIR,
        # 工程基础设施
        SCRIPTS_DIR,
        DOCS_DIR,
    ]


if __name__ == "__main__":
    print(f"ROOT_DIR (workspace) = {ROOT_DIR}")
    print(f"APP_DIR  (platform)  = {APP_DIR}")
    print(f"\n共享资产:")
    print(f"  DATA_DIR    = {DATA_DIR.relative_to(ROOT_DIR)}")
    print(f"  MODELS_DIR  = {MODELS_DIR.relative_to(ROOT_DIR)}")
    print(f"  RUNS_DIR    = {RUNS_DIR.relative_to(ROOT_DIR)}")
    print(f"\n端私有资产:")
    print(f"  CONFIGS_DIR = {CONFIGS_DIR.relative_to(ROOT_DIR)}")
    print(f"  LOGGING_DIR = {LOGGING_DIR.relative_to(ROOT_DIR)}")

    print(f"\n要初始化的目录共 {len(get_dirs_to_initialize())} 个:")
    for d in get_dirs_to_initialize():
        print(f"  - {d.relative_to(ROOT_DIR)}")
```

## Step 1.2:创建 4 个 `__init__.py`

**为什么需要**:让 `from odp_platform.common.paths import ...` 这样的导入能工作,Python 包需要 `__init__.py`。

**用编辑器**创建以下 4 个文件:

**文件 1**:`apps/platform/src/odp_platform/__init__.py`

```python
"""
ODPlatform - 通用目标检测开发平台

Public API 入口。具体子模块:
- odp_platform.common: 基础工具(路径/日志/字符串/系统/性能)
- odp_platform.cli: 命令行入口
"""

from odp_platform._version import __version__

__all__ = ["__version__"]
```

**文件 2**:`apps/platform/src/odp_platform/_version.py`

```python
"""版本信息——单一来源,被 pyproject.toml 和 odp_platform/__init__.py 共同引用"""

__version__ = "0.1.0"
```

**文件 3**:`apps/platform/src/odp_platform/common/__init__.py`

```python
"""common - 基础工具层"""
```

**文件 4**:`apps/platform/src/odp_platform/cli/__init__.py`

```python
"""cli - 命令行入口"""
```

## Step 1.3:测试 `paths.py`

**关于 `PYTHONPATH`**:在阶段 7 把项目装成包之前,运行需要 import 项目代码的命令时,要临时告诉 Python 包在哪。

🐧 **Linux / macOS / Git Bash**:
```bash
PYTHONPATH=apps/platform/src python -c "
from odp_platform.common.paths import ROOT_DIR, APP_DIR, get_dirs_to_initialize
print(f'ROOT_DIR  = {ROOT_DIR}')
print(f'APP_DIR   = {APP_DIR}')
print(f'共 {len(get_dirs_to_initialize())} 个待初始化目录')
"
```

🪟 **Windows PowerShell**:
```powershell
$env:PYTHONPATH = "apps/platform/src"
python -c "from odp_platform.common.paths import ROOT_DIR, APP_DIR, get_dirs_to_initialize; print(f'ROOT_DIR  = {ROOT_DIR}'); print(f'APP_DIR   = {APP_DIR}'); print(f'共 {len(get_dirs_to_initialize())} 个待初始化目录')"
```

预期输出(Windows 路径会是 `C:\xxx\ODPlatform` 形式,正常):

```
ROOT_DIR  = /xxx/ODPlatform
APP_DIR   = /xxx/ODPlatform/apps/platform
共 17 个待初始化目录
```

## Step 1.4:git commit

```
git add apps/platform/src/odp_platform/__init__.py
git add apps/platform/src/odp_platform/_version.py
git add apps/platform/src/odp_platform/common/__init__.py
git add apps/platform/src/odp_platform/common/paths.py
git add apps/platform/src/odp_platform/cli/__init__.py
git commit -m "feat(common): add paths.py with marker-file ROOT_DIR + APP_DIR"
```

✅ **检查点**:`git log --oneline` 看到 **2 次** commit。

---

# 阶段 2:`logging_utils.py`(专业日志)

**为什么需要这个模块**:真实工程项目几乎不直接用 `print`——`print` 没时间戳、没级别(成功/失败/警告)、不能存文件、没颜色、关掉终端记录就没了。所有输出都应该走 **logger**。

**核心设计哲学(贯穿后续所有课程,务必理解)**:

- **业务模块**(`paths`、`system_utils`、`init_project`,以及未来的 `train`/`val`……):顶部只写一行 `logger = logging.getLogger(__name__)`,**不配 handler**,只管 `logger.info("...")` 发声。
- **基础设施模块**(`logging_utils.py`):提供 `get_logger()`,把"控制台 + 文件"两个 handler 挂到**根 logger `"odp_platform"`** 上。
- **CLI 入口**(`init_project` 的函数最开头):**调用一次** `get_logger()` 装好 handler。之后整个进程里所有 `getLogger(__name__)` 的子 logger,都会通过 Python 的**冒泡机制**走到根 logger,由根 logger 的 handler 统一处理。

> 一句话:**业务模块负责"发声",不负责"配音响"。** 这样业务模块零配置,日志格式里还能自动看出每条日志来自哪个模块(`%(name)s` 字段)。

## Step 2.1:安装 colorlog

```
pip install colorlog
```

> 如果网络好,可以一次性把后面要用的也装了:`pip install colorlog psutil torch ultralytics`(torch+ultralytics 较大,2-4 GB,网络慢可以等到阶段 4 再装)。

## Step 2.2:创建 `logging_utils.py`

**用编辑器**创建 `apps/platform/src/odp_platform/common/logging_utils.py`:

```python
#!/usr/bin/env python
# -*- coding:utf-8 -*-
# @FileName  : logging_utils.py
# @Project   : ODPlatform
# @Function  : 高度可配置的日志工具,创建独立日志文件 + 彩色控制台输出
#
# 设计哲学:
#   - 业务模块: 顶部一行 logger = logging.getLogger(__name__), 不配 handler
#   - 本模块: 提供 get_logger(), 把 handler 挂到根 logger "odp_platform" 上
#   - CLI 入口: 调用一次 get_logger() 完成 handler 装配,
#               之后所有 getLogger(__name__) 通过冒泡机制自动继承

import logging
import sys
import platform
from datetime import datetime
from pathlib import Path
from typing import Optional
from colorlog import ColoredFormatter


# 项目根 logger 名 = 顶层 Python 包名
# 业务模块写 getLogger("odp_platform.xxx.yyy") 会通过冒泡找到这里挂的 handler
ROOT_LOGGER_NAME: str = "odp_platform"


def get_logger(
    base_path: Path,
    log_type: str = "general",
    model_name: Optional[str] = None,
    log_level: int = logging.INFO,
    temp_log: bool = False,
    encoding: str = "utf-8",
    logger_name: str = ROOT_LOGGER_NAME,
) -> logging.Logger:
    """
    配置项目【根 logger】, 挂上 console + file handler。

    设计要点:
        - 默认 logger_name = "odp_platform" (顶层包名),
          与业务模块的 getLogger(__name__) 形成 logger 树
        - 业务模块的日志会通过冒泡机制自动来到这里, 被本函数挂的 handler 处理
        - 重复调用幂等 (检查 handlers 是否已配置, 避免重复挂)

    Args:
        base_path: 日志根目录(通常是 paths.LOGGING_DIR)
        log_type: 日志类型,如 "init_project" / "train" / "val"
        model_name: 模型名(如 "yolo11n"),会拼到日志文件名里
        log_level: 日志级别(默认 INFO)
        temp_log: 是否标记为临时日志(文件名前缀变成 "temp")
        encoding: 文件编码(默认 utf-8)
        logger_name: 要配置的 logger 名 (默认是根 logger "odp_platform")

    Returns:
        配置好的 logging.Logger 实例
    """
    # ============================================================
    # 1. 获取/复用命名 Logger
    # ============================================================
    logger = logging.getLogger(logger_name)
    # 幂等保护: getLogger 是 singleton——同一个 logger_name 多次调用拿到同一对象。
    # 第一次调用配置好 handler 后, 后续调用直接返回, 避免重复挂多份 handler
    # (重复挂 handler 会导致同一条日志被打印 N 遍)。
    if logger.handlers:
        return logger

    logger.setLevel(log_level)
    logger.propagate = False  # 根 logger 不再向上冒泡(避免与 Python 默认 root logger 重复输出)

    # ============================================================
    # 2. 准备日志目录
    # ============================================================
    log_dir: Path = base_path / log_type
    log_dir.mkdir(parents=True, exist_ok=True)

    # ============================================================
    # 3. 构造日志文件名: <prefix>_<timestamp>[_<model>].log
    # ============================================================
    timestamp: str = datetime.now().strftime("%Y%m%d-%H%M%S-%f")[:21]
    prefix = "temp" if temp_log else log_type.replace("_", "-")

    filename_parts = [prefix, timestamp]
    if model_name:
        safe_model = "".join(
            c if c.isalnum() or c in "_-" else "_" for c in model_name
        )
        filename_parts.append(safe_model)
    log_file: Path = log_dir / ("_".join(filename_parts) + ".log")

    # ============================================================
    # 4. 文件 Handler(完整格式, 含 logger 名)
    # ============================================================
    # %(name)s 字段会输出 logger 名, 比如 "odp_platform.cli.init_project"
    file_formatter = logging.Formatter(
        fmt="%(asctime)s - %(name)s - %(levelname)-8s - "
            "%(filename)s:%(lineno)d - %(funcName)s - %(message)s",
        datefmt="%Y-%m-%d %H:%M:%S",
    )
    file_handler = logging.FileHandler(log_file, encoding=encoding)
    file_handler.setLevel(log_level)
    file_handler.setFormatter(file_formatter)
    logger.addHandler(file_handler)

    # ============================================================
    # 5. 控制台 Handler(彩色 + 紧凑格式)
    # ============================================================
    console_formatter = ColoredFormatter(
        "%(log_color)s%(asctime)s%(reset)s "
        "%(log_color)s[%(levelname)-8s]%(reset)s "
        "%(cyan)s%(filename)-25s%(reset)s:"
        "%(blue)s%(lineno)-4d%(reset)s "
        "%(log_color)s│ %(message)s%(reset)s",
        datefmt="%Y-%m-%d %H:%M:%S",
        log_colors={
            "DEBUG":    "white",
            "INFO":     "green",
            "WARNING":  "yellow",
            "ERROR":    "red",
            "CRITICAL": "bold_red,bg_white",
        },
        style='%'
    )
    console_handler = logging.StreamHandler(sys.stdout)
    console_handler.setLevel(log_level)
    console_handler.setFormatter(console_formatter)
    logger.addHandler(console_handler)

    # ============================================================
    # 6. 初始化信息
    # ============================================================
    logger.info('=' * 60)
    logger.info(f"日志系统初始化完成")
    logger.info(f"运行环境: {platform.system()} {platform.release()}")
    logger.info(f"阶段类型: {log_type}")
    logger.info(f"日志文件: {log_file}")
    logger.info(f"日志级别: {logging.getLevelName(log_level)}")
    logger.info(f"模型名称: {model_name or '无'}")
    logger.info('=' * 60)

    return logger


if __name__ == "__main__":
    # 模块自测——演示"基础设施装 handler + 业务模块发声"的标准用法
    from odp_platform.common.paths import LOGGING_DIR

    # 第一步: 装配根 logger 的 handler(这是 CLI 入口该做的事, 这里只是自测演示)
    get_logger(
        base_path=LOGGING_DIR,
        log_type="test",
        temp_log=True,
    )

    # 第二步: 业务代码就这样写——拿一个 __name__ logger, 直接发声
    test_logger = logging.getLogger(__name__)
    test_logger.debug("这是 DEBUG (默认 INFO 级别看不到)")
    test_logger.info("这是 INFO")
    test_logger.warning("这是 WARNING")
    test_logger.error("这是 ERROR")
```

## Step 2.3:测试

🐧 **Linux / macOS / Git Bash**:
```bash
PYTHONPATH=apps/platform/src python apps/platform/src/odp_platform/common/logging_utils.py
```

🪟 **Windows PowerShell**:
```powershell
$env:PYTHONPATH = "apps/platform/src"
python apps/platform/src/odp_platform/common/logging_utils.py
```

应看到彩色日志输出,同时生成 `apps/platform/logging/test/temp_xxx.log` 文件。

> Windows 彩色显示:用 **Windows Terminal** 或 **PowerShell 7+** 或 **Git Bash** 可完美显示彩色。如出现 `[32m` 这样的乱码,请换 Windows Terminal。

## Step 2.4:git commit

```
git add apps/platform/src/odp_platform/common/logging_utils.py
git commit -m "feat(common): add logging_utils.py with colored console + file output"
```

✅ **检查点**:`git log --oneline` 看到 **3 次** commit。

---

# 阶段 3:`string_utils.py`(中英文对齐)

**为什么需要这个模块**:Python 字符串的 `<25` 这类对齐计算的是**字符数**,但中文字符在终端**显示宽度是 2**(英文是 1)。所以含中文的表格用标准对齐会错位。这个模块正确处理 CJK(中日韩)字符的显示宽度。

## Step 3.1:创建 `string_utils.py`

**用编辑器**创建 `apps/platform/src/odp_platform/common/string_utils.py`:

```python
#!/usr/bin/env python
# -*- coding:utf-8 -*-
# @FileName  : string_utils.py
# @Project   : ODPlatform
# @Function  : 字符串工具——CJK 感知的对齐 / 表格格式化


def get_display_width(text: str) -> int:
    """
    计算字符串的实际显示宽度。

    Examples:
        >>> get_display_width("hello")
        5
        >>> get_display_width("你好")
        4
        >>> get_display_width("hi你好")
        6
    """
    return sum(2 if _is_wide_char(c) else 1 for c in text)


def _is_wide_char(char: str) -> bool:
    """判断单字符是否为宽字符(占 2 个显示宽度)"""
    if '\u4e00' <= char <= '\u9fff':  # 中日韩统一表意文字
        return True
    if '\u3000' <= char <= '\u303f':  # 中日韩标点符号
        return True
    if '\uff00' <= char <= '\uffef':  # 全角 ASCII
        return True
    if '\u3400' <= char <= '\u4dbf':  # 中日韩扩展 A
        return True
    if '\u3040' <= char <= '\u309f':  # 平假名
        return True
    if '\u30a0' <= char <= '\u30ff':  # 片假名
        return True
    if '\uac00' <= char <= '\ud7af':  # 韩文音节
        return True
    return False


def pad_to_width(text: str, width: int, align: str = 'left') -> str:
    """
    将字符串填充到指定显示宽度。

    Args:
        text: 输入字符串
        width: 目标显示宽度
        align: 'left' / 'right' / 'center'

    Examples:
        >>> pad_to_width("hello", 10)
        'hello     '
        >>> pad_to_width("你好", 10)
        '你好      '
    """
    current = get_display_width(text)
    padding = width - current
    if padding <= 0:
        return text

    if align == 'right':
        return ' ' * padding + text
    elif align == 'center':
        left = padding // 2
        right = padding - left
        return ' ' * left + text + ' ' * right
    else:
        return text + ' ' * padding


def format_table_row(columns: list, widths: list, aligns: list = None) -> str:
    """格式化表格一行"""
    if aligns is None:
        aligns = ['left'] * len(columns)
    assert len(columns) == len(widths) == len(aligns), "列数 / 宽度 / 对齐数必须一致"

    parts = [
        pad_to_width(str(col), w, a)
        for col, w, a in zip(columns, widths, aligns)
    ]
    return ' '.join(parts)


def format_table_separator(widths: list, char: str = '-') -> str:
    """生成与表格列宽匹配的分隔线"""
    total = sum(widths) + len(widths) - 1
    return char * total


if __name__ == "__main__":
    print("=== string_utils 自测 ===\n")

    cases = [("hello", 5), ("你好", 4), ("hi你好", 6), ("类别名称", 8)]
    for text, expected in cases:
        actual = get_display_width(text)
        status = "✓" if actual == expected else "✗"
        print(f"  {status} '{text}' → {actual} (期望 {expected})")

    print("\n=== 表格对齐示例 ===")
    widths = [12, 20, 6]
    aligns = ['left', 'left', 'right']
    print(format_table_row(['ID', '类别名称', '数量'], widths, aligns))
    print(format_table_separator(widths))
    print(format_table_row(['0', 'head', '60'], widths, aligns))
    print(format_table_row(['1', 'ordinary_clothes', '59'], widths, aligns))
    print(format_table_row(['2', '反光衣', '116'], widths, aligns))
```

## Step 3.2:测试

🐧 **Linux / macOS / Git Bash**:
```bash
PYTHONPATH=apps/platform/src python apps/platform/src/odp_platform/common/string_utils.py
```

🪟 **Windows PowerShell**:
```powershell
$env:PYTHONPATH = "apps/platform/src"
python apps/platform/src/odp_platform/common/string_utils.py
```

预期输出(中英文混排完美对齐):

```
=== string_utils 自测 ===

  ✓ 'hello' → 5 (期望 5)
  ✓ '你好' → 4 (期望 4)
  ✓ 'hi你好' → 6 (期望 6)
  ✓ '类别名称' → 8 (期望 8)

=== 表格对齐示例 ===
ID           类别名称                 数量
--------------------------------------
0            head                     60
1            ordinary_clothes         59
2            反光衣                  116
```

> Windows 如果中文还是错位:确认终端字体是等宽中文字体(如 Sarasa Mono SC),且终端编码为 UTF-8(`[Console]::OutputEncoding = [System.Text.Encoding]::UTF8`)。

## Step 3.3:git commit

```
git add apps/platform/src/odp_platform/common/string_utils.py
git commit -m "feat(common): add string_utils.py for CJK-aware text alignment"
```

✅ **检查点**:`git log --oneline` 看到 **4 次** commit。

---

# 阶段 4:`system_utils.py`(环境快照)

**为什么需要这个模块**:训练出一个模型后,三个月后别人想复现,需要知道当时的环境——操作系统、Python/PyTorch/Ultralytics 版本、CPU/内存、GPU/显存/CUDA。这个模块负责采集并打印这份"环境快照"。`torch`、`psutil`、`ultralytics` 都做成**软依赖**(没装也能优雅降级,不报错)。

## Step 4.1:安装依赖

```
pip install psutil torch ultralytics
```

> torch + ultralytics 较大(2-4 GB)。如果暂时不需要训练,**可以不装 torch/ultralytics**,本模块会自动降级显示 "Unknown (未安装)"。

## Step 4.2:创建 `system_utils.py`

**用编辑器**创建 `apps/platform/src/odp_platform/common/system_utils.py`:

```python
#!/usr/bin/env python
# -*- coding:utf-8 -*-
# @FileName  : system_utils.py
# @Project   : ODPlatform
# @Function  : 环境快照——OS / Python / PyTorch / CPU / GPU / 内存

from __future__ import annotations

import os
import platform
import time
import logging
from typing import Optional

from odp_platform.common.string_utils import pad_to_width

# 业务模块标准写法
logger = logging.getLogger(__name__)


def _format_size(bytes_size) -> str:
    """简洁的字节单位格式化"""
    if not bytes_size or not isinstance(bytes_size, (int, float)):
        return "N/A"
    if bytes_size >= 1024 ** 3:
        return f"{bytes_size / (1024 ** 3):.2f} GB"
    if bytes_size >= 1024 ** 2:
        return f"{bytes_size / (1024 ** 2):.2f} MB"
    return f"{bytes_size / 1024:.2f} KB"


def get_basic_device_info() -> dict:
    """返回结构化的环境信息字典"""
    cpu_name = platform.processor() or platform.machine() or "Unknown CPU"
    cpu_cores = os.cpu_count() or "Unknown"

    # 内存(psutil 是软依赖)
    try:
        import psutil
        memory = psutil.virtual_memory()
        total_ram = _format_size(memory.total)
        available_ram = _format_size(memory.available)
        ram_usage = f"{memory.percent:.1f}%"
    except ImportError:
        total_ram = "Unknown (psutil 未安装)"
        available_ram = "Unknown"
        ram_usage = "Unknown"

    # PyTorch / Ultralytics(软依赖)
    try:
        import torch
        torch_version = torch.__version__
        cuda_available = torch.cuda.is_available()
        gpu_count = torch.cuda.device_count() if cuda_available else 0
        _torch = torch  # 缓存模块引用, 后面 GPU 详情段直接用, 不再重复 import
    except ImportError:
        torch_version = "Unknown (torch 未安装)"
        cuda_available = False
        gpu_count = 0
        _torch = None

    try:
        from ultralytics import __version__ as ultralytics_version
    except ImportError:
        ultralytics_version = "Unknown (ultralytics 未安装)"

    gpu_info = {
        "CUDA可用": cuda_available,
        "GPU数量": gpu_count,
    }
    if cuda_available and _torch is not None:
        for i in range(gpu_count):
            gpu_info[f"GPU_{i}"] = _torch.cuda.get_device_name(i)
            gpu_info[f"GPU_{i}_显存"] = _format_size(
                _torch.cuda.get_device_properties(i).total_memory
            )

    return {
        "系统信息": {
            "操作系统": f"{platform.system()} {platform.release()} ({platform.machine()})",
            "主机名": platform.node(),
            "Python版本": platform.python_version(),
            "PyTorch版本": torch_version,
            "Ultralytics版本": ultralytics_version,
            "当前时间": time.strftime("%Y-%m-%d %H:%M:%S"),
        },
        "CPU信息": {
            "CPU型号": cpu_name,
            "核心数": cpu_cores,
        },
        "内存信息": {
            "总内存": total_ram,
            "可用内存": available_ram,
            "使用率": ram_usage,
        },
        "GPU信息": gpu_info,
    }


def log_device_info(target_logger: Optional[logging.Logger] = None) -> dict:
    """把环境信息打印到 logger。

    Args:
        target_logger: 可选, 指定的 logger 实例。
                       None 时使用本模块的 __name__ logger(推荐——
                       业务方调用时, 通过冒泡机制走到根 logger 的 handler)。
    """
    log = target_logger if target_logger is not None else logger

    info = get_basic_device_info()

    log.info("🚀 运行环境概览".center(60))
    log.info("=" * 60)

    key_width = 16

    for category, details in info.items():
        log.info(f"{category}".center(60))
        log.info("-" * 60)
        for key, value in details.items():
            padded_key = pad_to_width(key, key_width)
            log.info(f"  {padded_key}: {value}")

    return info


if __name__ == "__main__":
    # 自测——这里用 basicConfig 是因为自测脚本里没走完整 CLI 入口,
    # 真实使用中, CLI 入口已经调过 get_logger(), 这里不需要做任何配置
    logging.basicConfig(level=logging.INFO, format="%(message)s")
    log_device_info()
```

> 设计要点:`logger = logging.getLogger(__name__)` 是从今天开始所有业务模块的标准开头;`log_device_info` 默认用本模块自己的 logger,通过冒泡走到根 logger 的 handler。

## Step 4.3:测试

🐧 **Linux / macOS / Git Bash**:
```bash
PYTHONPATH=apps/platform/src python apps/platform/src/odp_platform/common/system_utils.py
```

🪟 **Windows PowerShell**:
```powershell
$env:PYTHONPATH = "apps/platform/src"
python apps/platform/src/odp_platform/common/system_utils.py
```

应打印出结构化的系统/CPU/内存/GPU 信息。

## Step 4.4:git commit

```
git add apps/platform/src/odp_platform/common/system_utils.py
git commit -m "feat(common): add system_utils.py for environment snapshot"
```

✅ **检查点**:`git log --oneline` 看到 **5 次** commit。

---

# 阶段 5:`performance_utils.py`(计时装饰器)

**为什么需要这个模块**:经常想知道某个函数跑了多久。与其每处手写 `start = time.time()`,不如做一个**装饰器**,加一行 `@time_it()` 就自动测量并打印,还能跑 N 次取平均。

## Step 5.1:创建 `performance_utils.py`

**用编辑器**创建 `apps/platform/src/odp_platform/common/performance_utils.py`:

```python
#!/usr/bin/env python
# -*- coding:utf-8 -*-
# @FileName  : performance_utils.py
# @Project   : ODPlatform
# @Function  : 性能测量工具——@time_it 装饰器

import logging
import time
from functools import wraps

# 业务模块标准写法
logger = logging.getLogger(__name__)


def time_it(
    iterations: int = 1,
    name: str = None,
    logger_instance: logging.Logger = None,
):
    """
    通用执行时间测量装饰器。

    Examples:
        @time_it()
        def my_func(): ...

        @time_it(iterations=10, name="批量推理")
        def infer_batch(): ...

        # 指定 logger:
        @time_it(logger_instance=logging.getLogger("my_module"))
        def custom(): ...
    """
    # 默认用本模块的 __name__ logger;
    # 调用方可以传一个特定 logger 进来(比如希望日志带着调用方的 logger 名)
    log = logger_instance if logger_instance is not None else logger

    def _format_time_auto_unit(seconds: float) -> str:
        """自动选择合适单位"""
        if seconds < 0.001:
            return f"{seconds * 1_000_000:.3f} 微秒"
        elif seconds < 1.0:
            return f"{seconds * 1000:.3f} 毫秒"
        elif seconds < 60:
            return f"{seconds:.2f} 秒"
        elif seconds < 3600:
            mins = seconds // 60
            secs = seconds % 60
            return f"{mins:.0f} 分钟 {secs:.2f} 秒"
        else:
            hours = seconds // 3600
            mins = (seconds % 3600) // 60
            secs = (seconds % 3600) % 60
            return f"{hours:.0f} 小时 {mins:.0f} 分钟 {secs:.2f} 秒"

    def decorator(func):
        @wraps(func)
        def wrapper(*args, **kwargs):
            display_name = name if name is not None else func.__name__
            total = 0.0
            result = None
            for _ in range(iterations):
                start = time.perf_counter()
                result = func(*args, **kwargs)
                end = time.perf_counter()
                total += (end - start)

            avg = total / iterations
            avg_str = _format_time_auto_unit(avg)

            if iterations == 1:
                log.info(
                    f"性能报告: '{display_name}' 执行耗时: {avg_str}"
                )
            else:
                total_str = _format_time_auto_unit(total)
                log.info(
                    f"性能报告: '{display_name}' 执行 {iterations} 次 | "
                    f"总耗时: {total_str} | 平均耗时: {avg_str}"
                )

            return result
        return wrapper
    return decorator


if __name__ == "__main__":
    # 自测时单独配一个简单 handler——真实使用中, CLI 入口已经配好了, 这里不用
    logging.basicConfig(level=logging.INFO, format="%(asctime)s - %(message)s",
                        datefmt="%H:%M:%S")

    @time_it(name="测试睡眠 0.1 秒")
    def test_sleep():
        time.sleep(0.1)

    @time_it(iterations=5, name="测试快函数")
    def test_fast():
        sum(range(1000))

    test_sleep()
    test_fast()
```

## Step 5.2:测试

```
python apps/platform/src/odp_platform/common/performance_utils.py
```

> 这个不需要设 `PYTHONPATH`,因为它没有 import 项目内其他模块。

## Step 5.3:git commit

```
git add apps/platform/src/odp_platform/common/performance_utils.py
git commit -m "feat(common): add performance_utils.py with @time_it decorator"
```

✅ **检查点**:`git log --oneline` 看到 **6 次** commit。

---

# 阶段 6:`init_project.py`(组装最终初始化脚本)

5 个 common 工具已全部就绪:

```
✅ paths.py             (路径常量 + ROOT/APP_DIR)
✅ logging_utils.py     (彩色日志, getLogger(__name__) 范式)
✅ string_utils.py      (CJK 对齐)
✅ system_utils.py      (环境快照)
✅ performance_utils.py (@time_it 装饰器)
```

现在把它们组装成最终的 `init_project.py`:创建所有运行时目录、打印环境快照、检查原始数据状态、输出对齐汇总表、报告耗时。

## Step 6.1:创建最终版 `init_project.py`

**用编辑器**创建 `apps/platform/src/odp_platform/cli/init_project.py`:

```python
#!/usr/bin/env python
# -*- coding:utf-8 -*-
# @FileName  : init_project.py
# @Project   : ODPlatform
# @Function  : 项目自动初始化——目录创建 + 数据集状态检查

import logging
from pathlib import Path
from typing import List

from odp_platform.common.paths import (
    ROOT_DIR,
    LOGGING_DIR,
    RAW_DATA_DIR,
    get_dirs_to_initialize,
)
from odp_platform.common.logging_utils import get_logger
from odp_platform.common.performance_utils import time_it
from odp_platform.common.system_utils import log_device_info
from odp_platform.common.string_utils import format_table_row, format_table_separator

LINE_WIDTH: int = 60

# 业务模块标准写法
logger = logging.getLogger(__name__)


def _check_raw_data_status() -> List[str]:
    """检查 raw 数据目录的状态"""
    raw_status: List[str] = []
    rel_raw = RAW_DATA_DIR.relative_to(ROOT_DIR)

    if not RAW_DATA_DIR.exists():
        logger.warning(
            f"原始数据集根目录不存在: {RAW_DATA_DIR}\n"
            f"请在该目录下创建以「数据集名称」命名的文件夹。"
        )
        raw_status.append(f"{rel_raw} 不存在 → 请创建并放入数据集")
    elif not any(RAW_DATA_DIR.iterdir()):
        logger.warning(
            f"原始数据集根目录为空: {RAW_DATA_DIR}\n"
            f"预期结构:\n"
            f"  {rel_raw}/<数据集名>/\n"
            f"  ├── images/\n"
            f"  └── annotations/"
        )
        raw_status.append(f"{rel_raw} 为空 → 请放入至少一个数据集")
    else:
        sub_dirs = [p for p in RAW_DATA_DIR.iterdir() if p.is_dir()]
        logger.info(f"原始数据集根目录就绪,检测到 {len(sub_dirs)} 个数据集文件夹")
        raw_status.append(f"{rel_raw} 就绪(包含 {len(sub_dirs)} 个数据集)")
        for sub in sorted(sub_dirs):
            raw_status.append(f"  • {sub.name}")
    return raw_status


@time_it(iterations=1, name="项目初始化", logger_instance=logger)
def initialize_project() -> None:
    """初始化项目核心目录 + 检查原始数据状态。

    本函数是 CLI 入口——负责装配根 logger 的 handler。
    装完之后, 整个进程里所有 getLogger(__name__) 都通过冒泡机制使用这套 handler。
    """
    # CLI 入口装配 handler (整个进程只装一次, get_logger 内部有幂等保护)
    get_logger(
        base_path=LOGGING_DIR,
        log_type="init_project",
        temp_log=False,
    )

    logger.info("开始初始化项目核心目录".center(LINE_WIDTH, '='))
    logger.info(f"项目核心目录为: {ROOT_DIR}")

    # 1. 打印环境快照——log_device_info 默认会用 system_utils 自己的 logger,
    #    通过冒泡同样到根 logger 的 handler, 一切自然
    log_device_info()

    # 2. 创建项目核心目录
    logger.info("检查并创建项目核心目录".center(LINE_WIDTH, '='))

    created: List[Path] = []
    existed: List[Path] = []

    for d in get_dirs_to_initialize():
        rel = d.relative_to(ROOT_DIR)
        if not d.exists():
            try:
                d.mkdir(parents=True, exist_ok=True)
                logger.info(f"成功创建: {rel}")
                created.append(d)
            except OSError as e:
                logger.error(f"创建失败 {rel}: {e}")
                raise SystemExit(1) from e
        else:
            logger.info(f"目录已存在: {rel}")
            existed.append(d)

    logger.info("项目核心目录检查创建完毕".center(LINE_WIDTH, '='))

    # 3. 检查原始数据目录状态
    logger.info("开始检查原始数据目录".center(LINE_WIDTH, '='))
    raw_status = _check_raw_data_status()

    # 4. 输出汇总信息
    logger.info("项目初始化汇总".center(LINE_WIDTH, '='))

    widths = [30, 12]
    aligns = ['left', 'right']
    logger.info(format_table_row(['目录', '状态'], widths, aligns))
    logger.info(format_table_separator(widths))
    for d in created:
        logger.info(format_table_row(
            [str(d.relative_to(ROOT_DIR)), '新建'], widths, aligns
        ))
    for d in existed:
        logger.info(format_table_row(
            [str(d.relative_to(ROOT_DIR)), '已存在'], widths, aligns
        ))

    if not created and not existed:
        logger.info("(本次没有任何目录变化)")

    logger.info("原始数据目录状态:")
    for status in raw_status:
        logger.info(f"  - {status}")

    logger.info("项目初始化完毕".center(LINE_WIDTH, '='))
    logger.info("下一步:把数据集放到 data/raw/ 下,然后运行数据转换脚本")
    logger.info("=" * LINE_WIDTH)


if __name__ == "__main__":
    initialize_project()
```

> 设计要点回顾:
> 1. 模块顶部 `logger = logging.getLogger(__name__)` —— 业务模块标准开头,永不变化。
> 2. `get_logger(...)` 放在 `initialize_project()` 函数最开头 —— CLI 入口装配 handler,整个进程只装一次。
> 3. `@time_it(logger_instance=logger)` 传本模块 logger,日志里能看出来自 `odp_platform.cli.init_project`。
> 4. `log_device_info()` 不传参,用 `system_utils` 自己的 logger,日志里能看出来自 `system_utils`。

## Step 6.2:创建开发阶段入口脚本

**为什么需要它**:阶段 7 才把项目装成包(`pip install -e`)。在那之前,直接 `python .../init_project.py` 会因为 Python 找不到 `odp_platform` 包而报错。临时方案是在仓库根放一个入口脚本,负责把 `src/` 加进 `sys.path`。

**用编辑器**创建 `scripts/init_project.py`:

```python
#!/usr/bin/env python
# -*- coding:utf-8 -*-
# @FileName  : scripts/init_project.py
# @Function  : 项目初始化的【开发阶段入口】(无需安装 package)
#
# 用法:
#   python scripts/init_project.py
"""ODPlatform 项目初始化入口(开发阶段)"""

import sys
from pathlib import Path

# 从仓库根定位 platform 的 src 目录
REPO_ROOT = Path(__file__).resolve().parent.parent
PLATFORM_SRC = REPO_ROOT / "apps" / "platform" / "src"

# 把 src 加到 sys.path 最前面(优先于已安装版本)
sys.path.insert(0, str(PLATFORM_SRC))

# 导入并运行真正的初始化函数
from odp_platform.cli.init_project import initialize_project

if __name__ == "__main__":
    initialize_project()
```

## Step 6.3:完整跑通

```
python scripts/init_project.py
```

🎉 应该看到:18 个目录被创建、完整环境快照、彩色日志、对齐的汇总表、性能报告。

注意日志里的 logger 名字段——能看出每条日志来自哪个模块:

```
2026-05-12 10:30:00 - odp_platform - INFO     - ... 日志系统初始化完成
2026-05-12 10:30:00 - odp_platform.cli.init_project - INFO     - ... 开始初始化
2026-05-12 10:30:00 - odp_platform.common.system_utils - INFO     - ... 运行环境概览
```

这正是 `getLogger(__name__)` 设计带来的好处。

## Step 6.4:git commit

```
git add apps/platform/src/odp_platform/cli/init_project.py
git add scripts/init_project.py
git commit -m "feat(cli): add init_project.py to bootstrap workspace"
```

✅ **检查点**:`git log --oneline` 看到 **7 次** commit。

---

# 阶段 7:工程化 —— `pyproject.toml` + `odp-init` 命令

**目标**:把 platform 装成一个 pip 可安装的包,这样可以直接敲 `odp-init` 命令(而不是 `python scripts/init_project.py`),并且以后再也不用设 `PYTHONPATH`。

**关于 build backend 的选择**:Python 现代打包标准(PEP 517)把"打包"做成可插拔的 backend。我们选 **hatchling**,原因:
1. **源码树干净**:`pip install -e .` 后不会在 `src/` 里冒出 `*.egg-info/` 目录(setuptools 的历史包袱),元数据只写到 `site-packages` 的 `*.dist-info/`。
2. **现代标准**:PEP 660 原生 editable install。
3. **更严格**:配置写错会早期报错,不会默默装出空包。

## Step 7.1:创建 `apps/platform/pyproject.toml`

**用编辑器**创建 `apps/platform/pyproject.toml`:

```toml
# apps/platform/pyproject.toml
# =============================================================================
# ODPlatform - platform 端配置 (hatchling backend)
# =============================================================================

[build-system]
requires = ["hatchling>=1.18.0"]
build-backend = "hatchling.build"


[project]
name = "odp-platform"
dynamic = ["version"]                              # 版本号从 _version.py 动态读
description = "ODPlatform - 目标检测开发平台核心引擎"
readme = "README.md"
requires-python = ">=3.10"
license = { text = "MIT" }
authors = [
    { name = "ODPlatform Team" },
]
keywords = ["object-detection", "yolo", "deep-learning", "ml-platform"]

classifiers = [
    "Development Status :: 3 - Alpha",
    "Intended Audience :: Developers",
    "License :: OSI Approved :: MIT License",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Topic :: Scientific/Engineering :: Artificial Intelligence",
]

dependencies = [
    "colorlog>=6.7.0",
    "psutil>=5.9.0",
    "torch>=2.0.0",
    "ultralytics>=8.0.0",
    "pyyaml>=6.0",
    "pydantic>=2.0",
]

[project.optional-dependencies]
dev = [
    "pytest>=7.4.0",
    "pytest-cov>=4.1.0",
    "ruff>=0.1.0",
    "mypy>=1.5.0",
]


# =============================================================================
# Entry-point Scripts (PEP 621 标准字段, 与 backend 无关)
# =============================================================================
[project.scripts]
odp-init = "odp_platform.cli.init_project:initialize_project"


# =============================================================================
# Hatchling 配置 (告诉它两件事: 包源码在哪、版本号从哪读)
# =============================================================================

# 告诉 hatchling 包源码在 src/odp_platform/
[tool.hatch.build.targets.wheel]
packages = ["src/odp_platform"]

# 动态版本号: 从 src/odp_platform/_version.py 的 __version__ 读
[tool.hatch.version]
path = "src/odp_platform/_version.py"
```

**用编辑器**创建 `apps/platform/README.md`:

```markdown
# apps/platform — ODPlatform 核心引擎

See workspace-level README for the project overview.
```

> 理解要点:`[project]` 和 `[project.scripts]` 是 **PEP 621 标准字段**,跟 backend 无关——换任何 backend 都不用改。只有最下面两段 `[tool.hatch.*]` 是 hatchling 专属。这就是 PEP 517 的"机制与元数据分离"。

## Step 7.2:创建顶层 `pyproject.toml`

**为什么有两份 pyproject**:
- `apps/platform/pyproject.toml`:描述一个**可安装的包**(有 `[build-system]`,是 `pip install -e .` 的对象)。
- 顶层 `pyproject.toml`:一个**workspace 配置容器**(没有 `[build-system]`,不会被安装,只是 ruff/mypy/pytest 自动读它的工具配置)。这是 Monorepo 常见的双层结构:内层管打包,外层管工具。

**用编辑器**创建 `pyproject.toml`(仓库根):

```toml
# pyproject.toml(顶层)
# =============================================================================
# ODPlatform Workspace 顶层配置
#
# 注意: 顶层这份 pyproject 不需要 [build-system]——
#       它不是一个可安装的 Python 包, 只是 workspace 级别的工具配置容器
#       (ruff / mypy / pytest 都会自动读这里)
# =============================================================================

[project]
name = "odplatform-workspace"
version = "0.1.0"
description = "ODPlatform - 通用目标检测开发平台 (Monorepo workspace root)"
readme = "README.md"
requires-python = ">=3.10"

[project.optional-dependencies]
dev = [
    "ruff>=0.1.0",
    "mypy>=1.5.0",
    "pre-commit>=3.4.0",
    "pytest>=7.4.0",
    "pytest-cov>=4.1.0",
]


# =============================================================================
# Ruff 配置
# =============================================================================
[tool.ruff]
line-length = 100
target-version = "py310"
src = [
    "apps/platform/src",
    "packages/shared-schemas/src",
]

[tool.ruff.lint]
select = ["E", "W", "F", "I", "B", "C4", "UP"]
ignore = ["E501"]

[tool.ruff.lint.isort]
known-first-party = ["odp_platform", "shared_schemas"]


# =============================================================================
# Mypy 配置
# =============================================================================
[tool.mypy]
python_version = "3.10"
warn_return_any = true
warn_unused_ignores = true
no_implicit_optional = true

[[tool.mypy.overrides]]
module = ["colorlog.*", "psutil.*", "ultralytics.*", "torch.*"]
ignore_missing_imports = true


# =============================================================================
# Pytest 配置
# =============================================================================
[tool.pytest.ini_options]
minversion = "7.0"
testpaths = [
    "apps/platform/tests",
    "tests",
]
python_files = ["test_*.py"]
addopts = ["-v", "--strict-markers", "--tb=short"]

markers = [
    "unit: 单元测试 (快,无 IO)",
    "integration: 集成测试 (有 IO)",
    "slow: 慢测试 (> 1s)",
]
```

**用编辑器**创建顶层 `README.md`(简单几行即可,被顶层 pyproject 引用):

```markdown
# ODPlatform

通用目标检测开发平台(Monorepo workspace root)。
```

## Step 7.3:安装 platform

```
pip install -e ./apps/platform
```

> 跨平台命令一致。pip 看到 `[build-system]` 里写着 hatchling,会在隔离环境里临时装一个 hatchling 来当 build backend,你不需要事先装 hatchling(这是 PEP 517 build isolation)。

**关键检查**:

🐧 **Linux / macOS / Git Bash**:
```bash
python -c "import odp_platform; print(odp_platform.__version__)"
which odp-init
odp-init
```

🪟 **Windows PowerShell**:
```powershell
python -c "import odp_platform; print(odp_platform.__version__)"
Get-Command odp-init
odp-init
```

🎉 `odp-init` 命令可用了。装完后**三种调用方式都能用**:

```
odp-init                                    # 生产/团队推荐(最简洁)
python -m odp_platform.cli.init_project     # 临时测试
python scripts/init_project.py              # 新人首跑/CI
```

## Step 7.4:git commit

```
git add apps/platform/pyproject.toml apps/platform/README.md
git commit -m "feat(platform): add pyproject.toml (hatchling) with odp-init entry-point"

git add pyproject.toml README.md
git commit -m "feat: add workspace-level pyproject.toml with shared tool config"
```

✅ **检查点**:`git log --oneline` 看到 **9 次** commit。

---

# 阶段 8:占位 README + 架构决策记录

回收阶段 0 的伏笔:三类目录里,A(代码)和 C(运行时)都已出生,现在轮到 B(占位目录)——给每个未来端写 README,目录顺带建出来。

## Step 8.1:创建占位 README

**用编辑器**逐个创建以下文件(内容都很短):

| 文件路径 | 内容要点 |
|---|---|
| `apps/web-backend/README.md` | 占位说明 + V1.1 启动 + FastAPI |
| `apps/web-frontend/README.md` | 占位说明 + V1.1 启动 |
| `apps/desktop/README.md` | 占位说明 + V2.0 启动 |
| `packages/shared-schemas/README.md` | 占位说明 + V1.1 + Pydantic schemas |
| `tests/e2e/README.md` | 占位说明 + 跨 app E2E 测试 |
| `data/README.md` | 数据集目录说明 |
| `models/pretrained/README.md` | 预训练模型目录说明 |
| `models/checkpoints/README.md` | 训练检查点目录说明 |
| `runs/README.md` | 训练运行结果目录说明 |
| `docs/architecture/README.md` | 架构决策记录(ADR)说明 |

模板示例(`apps/web-backend/README.md`):

```markdown
# apps/web-backend — Web 后端服务(占位)

> 🚧 **状态**: 占位,V1.1 启动开发

本子项目将提供 ODPlatform 的 Web 后端服务,基于:

- FastAPI(REST API)
- 复用 `apps/platform/` 的核心引擎(数据转换、推理)
- 共享 `packages/shared-schemas/` 的 Pydantic 数据模型

## 为什么现在是空的?

这一目录的存在是 **架构层面的预约**——
我们今天就给它留好位置,这样 V1.1 启动时:
- backend 团队知道往哪建
- platform 一行代码不用动
- 共享数据(`/data`、`/models`)直接复用

参考: `docs/architecture/ADR-001-monorepo.md`
```

其余 README 仿照此风格简写即可。

## Step 8.2:创建 ADR-001

**用编辑器**创建 `docs/architecture/ADR-001-monorepo.md`:

```markdown
# ADR-001: 采用 Monorepo + apps/ 布局

| 项目 | 内容 |
|---|---|
| 状态 | ✅ Accepted |
| 决策日期 | 2026-05-06 |

## 背景
ODPlatform V1.0 只有一个端,但 V1.1+ 要扩展为 3-4 个端。

## 备选方案
- 方案 A: 平铺布局(直觉做法)
- 方案 B: Monorepo + apps/ (采纳) ← 多打一层目录,未来扩展零成本
- 方案 C: Multi-repo

## 决定
方案 B: Monorepo + apps/

## 理由
1. 杠杆效应(今天免费,未来省 100 倍工作)
2. 共享代码方便
3. 跨端原子变更
4. 业界先例(Google / Meta / Uber)
5. 我们的规模合适

## 后果
- 正面: 多端启动零摩擦、跨端测试自然位置
- 负面: CI 稍慢、首次安装路径稍长
- 中性: 需要 marker file + 双层 pyproject

## 撤销条件
仓库 >100 万行 / 团队 >100 人 / 跨端发布周期严重冲突 → 重新评估

## 参考资料
- Monorepo vs Multi-repo
- Why Google uses a Monorepo
- src/ layout in Python projects (PyPA)
- PEP 517 / 621 / 660 — pyproject.toml 标准
```

## Step 8.3:git commit

```
git add apps/*/README.md packages/*/README.md tests/e2e/README.md
git add data/README.md models/*/README.md runs/README.md
git add docs/architecture/README.md
git commit -m "docs: add platform README + placeholder READMEs for future apps"

git add docs/architecture/ADR-001-monorepo.md
git commit -m "docs: add ADR-001 documenting Monorepo decision"
```

> Windows PowerShell 的 `git add apps/*/README.md` 一般正常工作(git 自己展开通配符);若报错就显式列出每个文件。

✅ **检查点**:`git log --oneline` 看到 **11 次** commit。**全部完成!**

---

# 阶段 9:健壮性验证(选做,推荐)

验证 marker file 模式真的"经得起重构"。把 `paths.py` 移出 `common` 一层:

🐧 **Linux / macOS / Git Bash**:
```bash
mv apps/platform/src/odp_platform/common/paths.py apps/platform/src/odp_platform/paths.py
```

🪟 **Windows PowerShell**:
```powershell
Move-Item apps/platform/src/odp_platform/common/paths.py apps/platform/src/odp_platform/paths.py
```

> 注意:移动后 `import` 路径会变,这里只是验证"`ROOT_DIR` 定位是否依赖 paths.py 自己的位置"。验证完**务必移回原位**:

🐧:
```bash
mv apps/platform/src/odp_platform/paths.py apps/platform/src/odp_platform/common/paths.py
```

🪟:
```powershell
Move-Item apps/platform/src/odp_platform/paths.py apps/platform/src/odp_platform/common/paths.py
```

结论:`ROOT_DIR` 的定位只依赖 `.odp-workspace` 在哪,不依赖 `paths.py` 在哪——这就是"为变化设计"。

---

# 总结:你今天完成了什么

## 完整 git 提交历史(11 次)

```
* (HEAD -> main) docs: add ADR-001 documenting Monorepo decision
* docs: add platform README + placeholder READMEs for future apps
* feat: add workspace-level pyproject.toml with shared tool config
* feat(platform): add pyproject.toml (hatchling) with odp-init entry-point
* feat(cli): add init_project.py to bootstrap workspace
* feat(common): add performance_utils.py with @time_it decorator
* feat(common): add system_utils.py for environment snapshot
* feat(common): add string_utils.py for CJK-aware text alignment
* feat(common): add logging_utils.py with colored console + file output
* feat(common): add paths.py with marker-file ROOT_DIR + APP_DIR
* chore: initial commit with .gitignore + .odp-workspace marker
```

## 你建立的能力栈

| 层面 | 内容 |
|---|---|
| 工具层 | paths / logging / string / system / performance |
| 架构层 | ROOT_DIR vs APP_DIR(共享 vs 私有) |
| 健壮性 | marker file 模式(为重构而设计) |
| 工程化 | src/ 布局 + hatchling pyproject + entry-point |
| 协作层 | conventional commits + 自解释 git log |
| 跨平台 | 同一套代码在 Linux/macOS/Windows 都能跑 |

---

# 附录:跨平台命令速查

| 操作 | Linux/macOS/Git Bash | Windows PowerShell |
|---|---|---|
| 创建目录 | `mkdir -p path/to/dir` | `New-Item -ItemType Directory -Path path/to/dir -Force` |
| 创建空文件 | `touch file` | `New-Item -ItemType File -Path file` |
| 删除文件 | `rm file` | `Remove-Item file` |
| 删除目录 | `rm -rf dir` | `Remove-Item -Recurse -Force dir` |
| 移动/重命名 | `mv src dst` | `Move-Item src dst` |
| 复制 | `cp src dst` | `Copy-Item src dst` |
| 查看命令路径 | `which cmd` | `Get-Command cmd` |
| 设环境变量(本会话) | `export VAR=val` | `$env:VAR = "val"` |
| 行延续符 | `\` | `` ` ``(反引号) |
| 终端清屏 | `clear` | `cls` |

**Windows 编码问题**(看到中文乱码或 git diff 异常时,加到 `$PROFILE`):

```powershell
[Console]::OutputEncoding = [System.Text.Encoding]::UTF8
$OutputEncoding = [System.Text.Encoding]::UTF8
git config --global core.quotepath false
```

---

*项目: ODPlatform | 对应实训日: D2(喂饭版) | 配套: 《D2 今日任务说明》*
